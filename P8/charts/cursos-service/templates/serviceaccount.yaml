{{- if .Values.serviceAccount.create }}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ .Values.serviceAccount.name }}
  namespace: {{ .Values.global.namespace | default "sa-p8" }}
  labels:
    {{- include "cursos-service.labels" . | nindent 4 }}
automountServiceAccountToken: false
{{- end }}

