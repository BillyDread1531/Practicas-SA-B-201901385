apiVersion: v1
kind: Service
metadata:
  name: {{ include "estudiantes-service.fullname" . }}
  namespace: {{ .Values.global.namespace | default "sa-p5" }}
  labels:
    {{- include "estudiantes-service.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  selector:
    {{- include "estudiantes-service.selectorLabels" . | nindent 4 }}
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.targetPort }}
      protocol: TCP
